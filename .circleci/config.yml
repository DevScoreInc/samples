# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
# Use a package of configuration called an orb.
orbs:
  # Declare a dependency on the beta/webhook
  DevScoreInc: beta/webhook@1.0.0
# Orchestrate or schedule a set of jobs
jobs:
  # Name the workflow "welcome"
  build:
    docker:
    - image: circleci/node:10
    steps:
    - run: echo "hi"
    - beta/webhook:
        endpoint: https://hooks.devscore.dev/function-runner/user-webhook/circleci/Zzg6YOtvdIhJ8QqzIDP3/?api_key=699f3620-64c8-11ea-b3a7-a52f03e82480
        endpoint_secret: safesecret123
    working_directory: ~/repo
